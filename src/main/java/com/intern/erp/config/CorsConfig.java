package com.intern.erp.config;

// Legacy CorsConfig disabled after consolidation into SecurityConfig.
// This class intentionally left without @Configuration or beans to avoid duplicate definitions.
public class CorsConfig {
    // No-op.
}