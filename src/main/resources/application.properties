spring.application.name=ERP

# Server Configuration
server.port=${PORT:8081}

# MongoDB Atlas connection
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://9922008101_db_user:00000@cluster0.mkptdj7.mongodb.net/mydatabase?retryWrites=true&w=majority&appName=Cluster0}

# Logging Configuration
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.com.intern.erp.outbox=DEBUG
logging.level.com.intern.erp.email=DEBUG

# ----------------------------
# SMTP Configuration (Ensure you use a valid App Password for Gmail)
# ----------------------------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME:harshasrikarthikeyathumuluri@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:vdbq wnhn izmg pxzu}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=${SPRING_MAIL_FROM:harshasrikarthikeyathumuluri@gmail.com}

# Outbox Processing
outbox.email.poll-interval-ms=5000
outbox.email.max-attempts=3

# Security Configuration
spring.security.user.name=${SPRING_SECURITY_USER_NAME:admin}
spring.security.user.password=${SPRING_SECURITY_USER_PASSWORD:admin123}
spring.security.user.roles=ADMIN


# Management endpoints
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Session Cookie Configuration for Cross-Site Support (Vercel -> Render)
server.servlet.session.cookie.same-site=none
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.http-only=true

